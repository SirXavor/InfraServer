apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns
  namespace: dns
data:

  Corefile: |
    .:53 {
        hosts /etc/coredns/hosts
        forward . 192.168.1.16 1.1.1.1
        cache 30
        reload
    }

  hosts: |
    192.168.1.70 boot.local
