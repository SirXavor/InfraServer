apiVersion: v1
kind: Service
metadata:
  name: infrawriter-frontend
  namespace: infrawriter
spec:
  selector:
    app: infrawriter-frontend
  ports:
    - name: http
      port: 80
      targetPort: 80
  type: ClusterIP