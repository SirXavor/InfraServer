apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: infrawriter-repo
  namespace: infrawriter
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi