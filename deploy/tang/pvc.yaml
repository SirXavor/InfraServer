apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: tang-data
  namespace: tang
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 1Gi
