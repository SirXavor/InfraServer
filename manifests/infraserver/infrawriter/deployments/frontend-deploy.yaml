apiVersion: apps/v1
kind: Deployment
metadata:
  name: infrawriter-frontend
  namespace: infrawriter
spec:
  replicas: 1
  selector:
    matchLabels:
      app: infrawriter-frontend
  template:
    metadata:
      labels:
        app: infrawriter-frontend
    spec:
      containers:
        - name: frontend
          image: xavor/infrawriter-frontend:1.0
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 80
              name: http
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi