apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: dnsmasq
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/SirXavor/InfraServer.git
    targetRevision: main
    path: manifests/infraserver/dnsmasq
  destination:
    server: https://kubernetes.default.svc
    namespace: dnsmasq
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
