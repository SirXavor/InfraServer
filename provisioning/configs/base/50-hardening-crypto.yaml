autoinstall:
  user-data:
    runcmd:
      - |
        #!/bin/bash
        set -euo pipefail
        SECLVL=${SECLVL:-2}
        MIN_TLS=${MIN_TLS:-TLSv1.2}
        GNUTLS_PROFILE=${GNUTLS_PROFILE:-"SECURE128:-VERS-SSL3.0:-VERS-TLS1.0:-VERS-TLS1.1"}
        echo "Aplicando hardening criptográfico"
        # aquí pegas tu script completo
