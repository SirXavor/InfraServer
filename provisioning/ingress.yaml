apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: provisioning
spec:
  ingressClassName: nginx
  rules:
  - host: boot.local
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: provisioning-server
            port:
              number: 80
